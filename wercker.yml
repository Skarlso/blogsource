box: golang
build:
    steps:
        - arjen/hugo-build:
            theme: hugo-octopress
            # version: "HEAD"
deploy:
    steps:
        - install-packages:
            packages: git
        - leipert/git-push:
            gh_oauth: $GIT_TOKEN
            repo: skarlso/skarlso.github.io
            branch: master
            basedir: public
