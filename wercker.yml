box: debian
build:
    steps:
        - arjen/hugo-build:
            theme: redlounge
deploy:
    steps:
        - git-push:
            gh_oauth: $GIT_TOKEN
            gh_pages: true
            gh_pages_domain: skarlso.github.io
            basedir: public
            repo: skarlso.github.io
            branch: master
